## Design

| 入口 | 默认 |
|------|------|
| `dapz proxy` / `DAPZ_OUTPUT_FORMAT` 未设 | `toon` |
| `Config::default` / builder 缺省 | `toon` |
| MCP / Agent SDK | 仍 TOON（不变） |
| IDE 要 JSON | `--output json` |

兼容：显式 `json` / `passthrough` 行为不变。
