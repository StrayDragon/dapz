# Tasks: align-proxy-output-formats

- [x] 1. Add `toon-format`; switch `value_to_toon` to `encode_default`
- [x] 2. Wire Proxy `passthrough` / `json` / `toon` like lspz
- [x] 3. Unit tests for three formats
- [x] 4. `just gen-bench` if ratios shift; `just qa`
- [x] 5. Validate + archive
