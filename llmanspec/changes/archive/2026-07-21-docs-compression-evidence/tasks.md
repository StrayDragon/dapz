# Tasks: docs-compression-evidence

- [x] 1. 新增 `fixtures/bench/scopes.json`、`fixtures/bench/exception.json`（各 ≥2 场景）
- [x] 2. `bench-report`：注册 Scopes/Exception；增加 TOON token 列与汇总
- [x] 3. `just bench-report` / `just gen-bench` 输出与 README 表同步
- [x] 4. `compress-demo` 可选补 scopes/exception 行
- [x] 5. Delta specs；`llman sdd validate docs-compression-evidence --strict --no-interactive`
- [x] 6. `just qa`
